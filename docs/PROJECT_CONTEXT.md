# Expense Tracker — Project Knowledge Base

> Purpose of this file: so any new chat (Claude.ai or Claude Code) can pick up this project with zero re-explaining. Update it at the end of every session — treat it as the source of truth, not the chat history.

## 1. What this is
A personal iOS expense tracker, built solo, to replace Splitwise for daily personal use (not for splitting bills with others — just tracking own spend). Core differentiator vs. existing apps: expenses are captured passively (email/SMS) and tagged via a single tap on a notification, instead of manual multi-field entry.

## 2. Roles
- **Prannoy = Product Manager.** Owns scope, priorities, daily usage/testing, and product decisions.
- **Claude (this chat) = tech lead / architect.** Owns technical decisions, planning, phase breakdown, and hands off implementation work to Claude Code.
- **Claude Code = builder.** Runs in the actual repo, writes/edits code, commits.

## 3. Hard platform constraints (don't relitigate these)
- **No bank API integration** — ruled out early (integration overhead too high for a personal project).
- **iOS cannot read SMS content** — no permission exists for this, for any app. This is an OS-level wall, not a gap in our build.
- **SMS workaround:** iOS Shortcuts automation ("When I receive a message containing X") → forwards text to a webhook we control. This is the only viable path for SMS capture on iPhone. Planned for Phase 4.
- **Email is fully capturable** via Gmail API (OAuth, read-only scope) — this is the primary automated-capture channel.

## 4. Product decisions made so far
- Platform: **iOS only** (native SwiftUI), not web, not Android.
- Local-first for Phase 1 (SwiftData), moving to Supabase for sync once backend exists.
- Notification-tap-to-tag is the core UX loop: user gets a push → taps → confirms category or marks "not an expense" + reason → this correction feeds back into future auto-categorization (few-shot, not a trained model).
- Currency assumed INR in UI (`.currency(code: "INR")`) — confirm/change if wrong.
- **Multi-Gmail-account sync: confirmed feasible, planned for Phase 2.** Reason: Prannoy has multiple bank accounts, and different banks/accounts send alert emails to different Gmail IDs — this is about bank-alert coverage, not a personal/work split. Gmail OAuth is per-account (standard, no blocker) — each connected Gmail gets its own token pair. Schema needs a `connected_accounts` table (email + encrypted access/refresh tokens), and every ingested expense gets tagged with which account it came from. Since the driver is coverage (not separating contexts), account source can stay invisible metadata rather than a user-facing filter — revisit only if a need to filter by account/bank surfaces later.

## 5. Phase roadmap (MVP = Phases 1–3)

| Phase | Purpose | PM focus | Status |
|---|---|---|---|
| 1. Manual capture (SwiftUI + SwiftData) | Prove daily usage habit before automating | Use it daily, flag friction | ✅ Built — needs Prannoy to run in Xcode & test |
| 2. Backend (Supabase) | Invisible plumbing — cloud storage, needed before Phase 3 can work. Includes `connected_accounts` table for multi-Gmail support (bank alerts land across multiple Gmail IDs) | Confirm app feels unchanged | Not started |
| 3. Gmail auto-capture + tag-via-notification | Core value prop: passive capture, 1-tap tagging | Extraction accuracy + tagging speed | Not started |
| 4. SMS capture (iOS Shortcuts) | Cover bank SMS alerts, thin addition on top of Phase 3 pipeline | One-time Shortcut setup | Not started |
| 5. Learning loop + reporting | Auto-tag accuracy improves over time; monthly/category insights | How much manual correction is still needed | Not started |

## 6. Tech stack
- **App:** SwiftUI, SwiftData (local), iOS native
- **Backend:** Supabase (Postgres + Auth + Edge Functions) — free tier
- **Classification/extraction:** Claude API called from a Supabase Edge Function
- **Push notifications:** APNs
- **Email ingestion:** Gmail API (OAuth, read-only)
- **SMS ingestion:** iOS Shortcuts automation → webhook (no native SMS API)

## 7. Repo structure
```
expense-tracker/
  docs/
    PROJECT_CONTEXT.md          <- this file
    ROADMAP.md                  <- phase timeline
  ios/
    ExpenseTracker/
      ExpenseTrackerApp.swift
      Models/Expense.swift
      Views/ExpenseListView.swift
      Views/AddEditExpenseView.swift
  backend/                      <- Phase 2, not yet created
  design/                       <- UI/UX, wireframes, Figma
```

## 8. Open questions / not yet decided
- Gmail OAuth credentials not yet set up (need Google Cloud project) — `connected_accounts` table now exists (Session 4) but nothing writes real tokens into it yet.
- No multi-currency handling yet, INR hardcoded
- `connected_accounts.access_token`/`refresh_token` are plaintext columns — must be encrypted (Supabase Vault/pgsodium or app-layer) before Phase 3 writes real OAuth tokens into them. Flagged as a security follow-up in Session 4, not yet actioned.
- Recurring groups/categories now have a create+view UI (Session 5, Account tab) — but no edit/delete UI yet for either (mockup itself doesn't show one either). `ExpenseGroupManager.updateGroup`/`deleteGroup` and `CategoryManager.deleteCategory` exist in the data layer, unused by any screen.
- Account tab's Connected accounts and Notifications sub-pages were built and compile against real tables (Session 5), but weren't visually verified in the simulator this session — see Session 5 log for what was and wasn't checked.
- Account tab is not wired into `MainTabView.swift` yet (still shows the Phase 1 stub) — deliberate, to avoid a guaranteed merge conflict with the other five screen-rebuild threads also touching that file. Needs one line changed at final integration: swap the Account `StubTabView` for `AccountView(authManager: authManager)` (see `Views/AccountView.swift`'s doc comment).

## 9. Session log
*(Append a dated entry each session — a few lines: what was built/decided, what's next.)*

- **2026-07-26 (Session 1):** Defined roadmap, ruled out bank integration and iOS SMS reading, built Phase 1 (SwiftUI + SwiftData CRUD app). Repo + KB structure created. Confirmed multi-Gmail-account sync is feasible (per-account OAuth), driven by multiple bank accounts alerting to different Gmail IDs — added `connected_accounts` table as a Phase 2 requirement, account source to stay invisible metadata (not a UI filter) unless that need surfaces later. Next: Phase 2 (Supabase backend).
- **2026-07-26 (Session 2):** Created GitHub repository structure with full project layout including Design folder. Generated initial docs, README, .gitignore, LICENSE. Ready to push to GitHub.
- **2026-07-27 (Session 3, in progress — items 1-3 of Phase 1 brief):** Working on branch `fix/phase1-e2e`. Found the working tree already had an in-progress, uncommitted rework of the Xcode project (new nested `ios/ExpenseTracker/ExpenseTracker.xcodeproj` structure replacing the old flat one, plus a partial attempt at the auth-token fix) — adopted it as canonical rather than discarding it. Nothing has been committed yet.
  - **Session/token handling (item 1) — done and verified.** `AuthManager` now captures `access_token`/`refresh_token`/`expires_in` from OTP verify, stores them in Keychain (not UserDefaults — they're live credentials), and auto-refreshes on launch if expired. `ExpenseManager` now attaches the real per-user access token as the `Authorization: Bearer` header (previously it just resent the anon key, which only "worked" because of a wide-open debug RLS policy — see below). On a 401, it refreshes once and retries.
  - **Login/OTP end-to-end (item 2) — done and verified**, necessarily together with item 1 since there's no way to prove token capture without a real login. Found and fixed a real bug: `verifyOTP` was calling Supabase's `/verify` with `type: "otp"`, which Supabase rejects (`token has expired or is invalid`) — the correct value is `type: "email"`. Confirmed via direct curl before touching app code.
  - **Add expense (item 3) — done and verified** as part of proving item 1 (needed a real write to confirm the header actually satisfies RLS). Verified the row in Supabase directly: `user_id` is the real Supabase auth UUID (`fd750254-...`), not an email string — this was one of the three bugs flagged at the start of the session, now confirmed fixed.
  - **Deviations / infra changes, all called out to Prannoy and approved before acting:**
    - Dropped the `debug_open_access_temp` RLS policy on `expenses` (`FOR ALL TO anon USING (true)`) — a real security hole letting anyone with the public anon key read/write/delete all users' data. Only the four real `auth.uid() = user_id` policies remain.
    - Deleted one stale test row that had been written through that backdoor with a fake local UUID.
    - Removed the "Skip Login (Debug)" bypass from `LoginView`/`AuthManager` — it can't work now that RLS is locked down, and real OTP is the path being verified.
    - Supabase's free tier blocks email template customization on its default mailer, so the OTP-code email had no visible code (magic-link only). Configured custom SMTP via Resend (free tier) and added `{{ .Token }}` to the confirmation/magic-link templates so the code shows up.
    - Resend's sandbox only delivers to the email you signed up with (`p.tugiti@gmail.com`), not `prannoy.tugiti@safebooks.ai` — so **`p.tugiti@gmail.com` was used as the test sign-in identity this session**, with Prannoy manually relaying the OTP code (Gmail MCP here is connected to the safebooks.ai account, not the personal one). This is a Resend-account limitation, not a code limitation — any email works once a domain is verified.
    - Raised `rate_limit_email_sent` from 2/hour to 100/hour (a leftover default-mailer throttle that persisted after switching to custom SMTP) — was blocking repeated test sends.
    - Fixed `.autocapitalization(.none)` (deprecated, silently inert) → `.textInputAutocapitalization(.never)` on the email field; it was auto-capitalizing the first letter of typed emails.
  - **Edit expense (item 4) — done and verified.** Tap-to-edit didn't exist at all before this session (rows were static, no navigation) — built `EditExpenseView` (pre-filled form, `PATCH` via `updateExpense`) and wired a tap gesture on each row. Verified by editing the test row's amount (999.00 → 450.00) through the real UI and confirming the new value in Supabase directly.
  - **Delete expense (item 5) — done and verified.** Swipe-to-delete was already wired to `ExpenseManager.deleteExpense` via the List's native `.onDelete`; confirmed it actually works end-to-end (swipe → reveal Delete → tap → row gone from both the UI and Supabase).
  - **Visual fidelity pass (item 6) — in progress, paused mid-way (usage limit).** Pulled design tokens (colors, fonts, card/chip/FAB styling) from `design/mockups/Expense Tracker.dc.html` into a new `Theme.swift`, and rebuilt the four in-scope screens to match:
    - **Login/OTP** — dark hero screen with radial glow, matching heading/subtitle, white input, 8-box OTP entry (Supabase sends 8-digit codes) with blue focus ring. Deliberately did **not** adopt the mockup's "Tally" brand name/logo mark or Google/phone-login buttons — kept "Expense Tracker" and email-only, since renaming the app is a product decision, not a styling one. Verified end-to-end with a real login.
    - **Home** — real month-grouped list (section header per month), "This Month" total card, styled expense cards with per-category icon+color tint, and a real floating 56×56pt blue FAB (previously a toolbar "+"). Added a 4-tab bar (Home/Ignored/Insights/Account) matching the mockup's nav, with Insights/Account as explicit stubs (no functionality) per the brief's "stub the nav tabs" instruction.
    - **Add/Edit sheets** — rebuilt as a shared `ExpenseFormSheet` component: colored category chips (icon + checkmark when selected, matching `design/ui_kit/components.md`'s Category Pill spec) instead of a native picker, ₹-prefixed amount field, bottom-sheet chrome with an X close button. Kept tap-to-edit and swipe-to-delete exactly as they were (per your explicit instruction not to change those interactions).
    - All of the above **re-verified working after the redesign**: real login, add (₹180 Coffee House), edit (in place), and swipe-to-delete, all confirmed against the actual Supabase rows, not just visually.
  - **Real bug found and fixed along the way:** the new Login screen's email field silently refused to focus — tap coordinates looked correct but nothing happened. Root-caused (via a long bisection, not guesswork) to a genuine SwiftUI hit-testing quirk: a `VStack` with only a *leading* `Spacer()` pushing content to one edge (no flexible space on the far side) breaks tap delivery to a `TextField` inside it, specifically when a hardware keyboard is connected (no on-screen keyboard to reconcile the hosting view's layout against). Fix: always pair a leading `Spacer()` with a bounded trailing one (`Spacer().frame(maxHeight: 40)`) so there's flexible space on both sides. Documented inline in `LoginView.swift` since this isn't obvious and could resurface in future screens.
  - **Known open issue, not yet root-caused:** the new pill-shaped 4-tab bar doesn't respond to simulator taps on "Ignored" (Home stays selected) even at coordinates that visually match the tab — tried multiple positions. The FAB and everything above the tab bar tap correctly at the same time, so input generally isn't broken; this looks specific to the tab bar. Given it's a stock, uncustomized SwiftUI `TabView`, my leading guess is an automation-environment quirk with this iOS version's new "Liquid Glass" floating tab bar rendering, not an app bug — but that's unconfirmed. **Needs a real device/manual tap check** before ruling out an actual issue.
  - **Not committed yet** — working tree has real changes on `fix/phase1-e2e`, nothing pushed or merged.
  - **Next:** finish visual fidelity (check the Ignored screen's new styling live, verify the tab-bar issue above on a real tap, review remaining screens against the mockup for spacing/color accuracy), then remaining Phase 1 scope (item 7).

- **2026-07-27 (Session 4, branch `feature/data-foundation`):** Built the data-layer prerequisite for the six parallel screen-rebuild threads (Home/Ignored/Add-Edit/Insights/Account/Login) that branch off this commit. This session **expanded scope beyond the original phase roadmap** — recurring groups, multi-type custom categories, and notification preferences were never in `docs/ROADMAP.md`, and `connected_accounts` was a Phase 2 item pulled forward into this branch instead. That's a scope call Prannoy made when scoping this session, not one to bury silently.
  - **Tooling note:** no Supabase MCP server was connected in this environment (CLAUDE.md section 7 assumes one). Used the Supabase **Management API** (`https://api.supabase.com/v1/projects/{ref}/database/query`) directly instead, authenticated with the personal access token already sitting in `.env.local` (`SUPABASE_ACCESS_TOKEN`) — same verification rigor (real queries against the live project), different transport. Worth actually connecting the Supabase MCP server per `SETUP.md` for future sessions.
  - **Migration:** `backend/supabase/migrations/20260727190000_add_transaction_types_and_supporting_tables.sql` — the first file ever checked into `backend/supabase/migrations/`. The existing `expenses` table has no baseline migration of its own (it was hand-applied via the SQL editor in Session 3); this file assumes that table already exists as shipped then.
  - **Naming decision: kept the `expenses` table and `Expense` Swift model as-is** (did not rename to `transactions`/`Transaction`). Reasoning: six screen threads branch off this exact commit and call `fetchExpenses`/`addExpense`/etc. immediately — a rename would touch every call site across all six branches for zero functional gain. The new `type` column (`expense`/`credit`/`investment`) and `expense_groups` table carry the mockup's three-transaction-type + recurring-group model within the same physical table.
  - **Real schema issue found and handled:** the live `expenses` table had a `CHECK` constraint (`expenses_category_check`) hardcoding the 8 expense-only category names. That constraint could not coexist with credit/investment categories sharing the same table, so it's dropped as part of this migration — category validity now lives in the new per-user `categories` table / app layer instead of a DB check constraint.
  - **Migration applied in two steps, per an explicit confirmation gate** (CLAUDE.md's live-infra caution rule): Part 1 (new, additive tables — `expense_groups`, `categories`, `connected_accounts`, `notification_preferences`, plus a `handle_new_user_categories` trigger that auto-seeds default categories for every future signup) was applied immediately since it touches nothing existing. Part 2 (the `expenses` ALTER — add `type`, add `group_id`, drop the old category check) was applied only after Prannoy explicitly confirmed via an in-chat prompt, since it touches the one real test row.
  - **Security follow-up flagged, not fixed:** `connected_accounts.access_token`/`refresh_token` are plaintext columns. No Gmail OAuth writer exists yet (that's Phase 3), so nothing real is stored there today — but before Phase 3 ships, these need encryption (Supabase Vault/pgsodium, or app-layer encryption before insert) rather than relying on RLS alone. Logged in section 8 above so it isn't forgotten.
  - **Swift changes:** extended `Expense.swift` in place with a new `TransactionType` enum (`expense`/`credit`/`investment`) and `type`/`groupId` fields (rather than introducing a renamed `Transaction` model — same reasoning as the table-naming decision). Added `ExpenseGroup.swift`, `Category.swift`, `ConnectedAccount.swift` (deliberately does **not** model `access_token`/`refresh_token` client-side — OAuth exchange is server-side, the client only ever needs status), and `NotificationPreferences.swift`.
  - **Manager refactor:** factored the networking boilerplate shared by every REST-backed manager (attach the real bearer token, retry once on 401 via `AuthManager.refreshSession`, surface the raw Supabase error body) out of `ExpenseManager` into a new `SupabaseManager` base class, so the four new managers (`ExpenseGroupManager`, `CategoryManager`, `ConnectedAccountManager`, `NotificationPreferencesManager`) don't each reimplement it. `ExpenseManager`'s public method signatures (`fetchExpenses`, `addExpense`, `updateExpense`, `deleteExpense`) are unchanged — verified by building the app and test target clean.
  - **Views touched — deliberately minimal, no screen UI rework:** only `AddExpenseView.swift` and `EditExpenseView.swift`, each getting `type`/`groupId` arguments added to their existing `Expense(...)` construction call (`.expense`/`nil` for new expenses added through the old form; preserved existing values when editing). `MainTabView.swift` was **not** wired up to instantiate the four new managers — that wiring is left to whichever screen-rebuild thread actually needs them, since the injection lifetime/scope is a UI decision this branch was told not to make.
  - **Verified (CLAUDE.md rule 2 — real rows, not assumed):** `xcodebuild build` and `build-for-testing` both succeeded clean. Every new table got a real row inserted and queried back against the live project: `expense_groups` ("Rent"), `categories` (the seed trigger produced 8 expense / 6 credit / 5 investment rows for the real test user), `connected_accounts` ("prannoy@gmail.com"), `notification_preferences` (one row for the test user). The new `expenses.group_id` FK was verified end-to-end by linking the existing test expense to the "Rent" group, confirming the join resolves, then reverting `group_id` back to null to leave the test row as originally found. The pre-existing test expense row was confirmed intact post-migration with `type` correctly defaulted to `'expense'`.
  - **Not verified:** the four new managers' Swift networking code paths were not exercised through a live, OTP-authenticated simulator session (that would need a full login flow) — the REST operations they perform were instead verified directly against the live database via the Management API. Their auth-attachment logic is the exact same `SupabaseManager.performRequest`/`authorizedRequest` code already proven end-to-end for `expenses` in Session 3, so this isn't new/untested logic, but it hasn't been driven through the actual UI. That's expected to happen naturally once the screen-rebuild threads wire these managers into real screens.
  - **Committed locally** on `feature/data-foundation` per the task's explicit instruction — not pushed, not merged.
  - **Next:** the six screen-rebuild threads can now branch off this commit. Each should wire up whichever of the new managers it needs (Account → `ConnectedAccountManager`/`NotificationPreferencesManager`/`CategoryManager`; Add/Edit → `CategoryManager`/`ExpenseGroupManager`; Insights → reads `type` for the savings computation).

- **2026-07-27 (Session 5, branch `feature/account-screen`):** Built the full Account tab (was a bare `StubTabView`), matching `design/mockups/Expense Tracker.dc.html` lines 290-509 — profile header + sign out, and five sub-pages, all pushed via a plain `NavigationStack` with a custom circular back button (matching the app's existing no-native-nav-bar convention) rather than the mockup's in-place screen-swap model.
  - **Merged `feature/data-foundation` into this branch first** (fast-forward — this branch hadn't diverged from that commit's parent) to get the `expense_groups`/`categories`/`connected_accounts`/`notification_preferences` managers and models built in Session 4.
  - **New files:** `Views/AccountView.swift` (root) + `Views/Account/{AccountComponents,RecurringGroupsView,NewGroupSheet,GroupDetailView,ConnectedAccountsView,CategoriesView,ExportView,NotificationsView}.swift` — one thread, kept together rather than split, since the sub-pages share components (`PillSegmentedControl`, `FlowLayout`, `AccountSubHeader`) that would otherwise need duplicating or a shared file anyway.
  - **Also touched `Utils/Theme.swift`** (allowed — not a `Views/` file) to add `creditFg`/`creditBg`/`investmentFg`/`investmentBg` and a `TransactionType.tint` extension, mirroring the existing `expenseFg`/`expenseBg` pair, so group/category type badges have a real color per the mockup's `TYPE_META` instead of falling back to a single accent color.
  - **Recurring groups:** list + detail + "New group" sheet, backed by `ExpenseGroupManager`. Detail totals and the linked-transactions list are computed client-side from the already-fetched `expenseManager.expenses` (filtered by `group_id`), not a separate query. No edit/delete UI — the mockup doesn't have one either, and it wasn't in the brief; `updateGroup`/`deleteGroup` remain unused in the data layer.
  - **Connected accounts:** list + CRUD backed by `ConnectedAccountManager`. **Deviation, called out per the brief:** "Add Google account" is an explicit manual-entry stub (a sheet that says outright "real Gmail OAuth is planned for Phase 3... nothing is actually connected to Gmail") — it just inserts an email/status row. Status can be changed via a row context menu and rows can be swiped to delete, so create/read/update/delete all exercise the real table.
  - **Categories:** expense/credit/investment tabs (`PillSegmentedControl`) + chip list (`FlowLayout`) + add-a-category flow, backed by `CategoryManager`. New categories get the mockup's own "Other" gray fallback (`#E2E2E2`/`#221F1F`) since neither the mockup nor the brief specifies a color picker for user-added categories.
  - **Export:** date-range chips (This Month/Last Month/Last 3 Months/All Time/Custom with native date pickers) filter the already-fetched `expenseManager.expenses` client-side, build a CSV string, write it to a temp file, and hand it to `UIActivityViewController` (wrapped as `ActivityView`). **Deviation, called out per the brief:** the mockup's `runExport` triggers a browser download; there's no browser here, so this uses the native iOS share sheet instead.
  - **Notifications:** two toggles (native `Toggle`, tinted — a very close visual match to the mockup's custom pill/knob control, not a "generic fallback") bound to `NotificationPreferencesManager`, upserting on every change. **Deviation, called out per the brief:** a caption under the toggles states plainly that these only persist a preference row — no push notification is actually sent on a new transaction or budget threshold; that's separate, unrelated future work.
  - **Not wired into `MainTabView.swift`** — same reasoning as every other screen-rebuild thread (six threads touching one file guarantees a merge conflict; left for final integration, a one-line swap of the Account `StubTabView` for `AccountView(authManager: authManager)`).
  - **Verification, and where it fell short of the full bar:**
    - `xcodebuild build` succeeded clean, both with the new files sitting unwired (real state) and with a temporary local-only wiring into `MainTabView.swift` used to test in-simulator (reverted before committing — never part of any commit).
    - **Login automation blocker, worked around:** this session's simulator automation couldn't drive the OTP login screen reliably (a text field that wouldn't focus until the simulator's hardware-keyboard passthrough was disabled, then a tab-bar/button tap-delivery quirk matching the *exact* unresolved issue flagged in Session 3 — "known open issue... needs a real device/manual tap check" — turned out to reproduce on plain rows too, not just the tab bar, confirming it's an automation-environment quirk rather than an app bug). Also discovered this session: multiple worktree sessions share the same physical simulators, so a build installed by one thread can silently get overwritten/fronted by another's — worth remembering for future sessions rather than assuming a screenshot reflects your own build. Resolved pragmatically: Prannoy logged in and drove navigation by hand (real clicks, which don't hit the automation quirk) on a simulator with this session's build installed, while Claude Code screenshotted after each step and verified data via the Supabase Management API.
    - **Verified working, UI + real data:** Account root (profile header, all 5 nav rows with live counts, sign out button). Recurring groups list + "New group" sheet — created a real group ("Investments", type investment, recurring, `cat_ref` null) confirmed via direct Supabase query. Group detail view (badges, total card, empty-transactions state) via the pre-existing "Rent" group. Categories tabs/chips/add flow — created two real categories ("ABC" expense, "XYZ" credit) confirmed via direct Supabase query, both correctly defaulted to the gray fallback color. Export page (range chips wrap correctly via `FlowLayout`, preview count/total matched the one real expense in the account).
    - **Not visually verified this session:** Connected accounts and Notifications sub-pages were never actually opened/screenshotted (time-boxed — Prannoy opted to merge now and pick this up later rather than spend more on the tap-automation workaround). Both compile and follow the exact same patterns (`AccountSubHeader`, list/toggle rows, manager CRUD calls) already proven working in the sibling sub-pages, but that's inference, not observed proof — flagged in section 8 above so it isn't forgotten.
    - Back-navigation was exercised successfully once (Rent group detail → Recurring groups list) via automated tap; the underlying mechanism (`@Environment(\.dismiss)` popping a `NavigationStack` push) is identical across every sub-page.
  - **Thread count note for Prannoy:** built as one thread (not split into groups/categories vs. accounts/export/notifications as the brief floated) — the shared components made splitting not worth the coordination overhead. If a future session wants to touch just one sub-page, the files are already separated enough to do that without the others.
  - **Committed on `feature/account-screen` and merged into `main`** per Prannoy's explicit instruction, once the Connected accounts/Notifications visual check was deferred rather than blocking the merge.
  - **Next:** wire `AccountView` into `MainTabView.swift` at final integration; visually verify Connected accounts + Notifications; decide whether recurring groups/categories need edit/delete UI beyond what's in the mockup.
